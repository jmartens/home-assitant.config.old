language: python
python:
  - "3.7"
before_install:
  - sudo apt-get install -y libudev-dev
  - ls -la
  - grep -r secret *.yaml
install:
  - pip3 install homeassistant
script:
  - hass -c . --script check_config
